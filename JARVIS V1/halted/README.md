# Halted

These scripts are kept so the history is not lost. Do not run them.

| File | Why it is here |
|---|---|
| `manage_cycle_2026-09-22.py` | Old manage loop. It still contains a 10-lot cap and stale kill levels. `STANDING_ORDERS.md` forbids that behavior. |
| `_pass_once.py` | Not in this folder at the 22:20 seal. Earlier that night `research/_pass_once.py` sent 10.0-lot orders with comment `J 10lot`. If that file reappears anywhere, do not run it. |

The live seat is `JARVIS V1/STANDING_ORDERS.md`.
